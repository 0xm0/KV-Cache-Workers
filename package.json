{
  "private": true,
  "name": "kv-cache-workers",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "0xm0",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
